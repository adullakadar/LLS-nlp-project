# LLS-nlp-project

Setup: 
1) Install python 3.11.9
1) Create a python environment (3.11.9) and use
2) Install packages from requirements.txt (pip install -r requirements.txt)
3) Obtain a mongo connection string from mongodb atlas and enter into .env (YT_API_KEY='_')
4) Obtain a youtube API connection key and insert into .env (MONGDB_KEY='_')
5) important on vscode: downgrade jupyter extension to 2025.8 if running code through notebooks
6) 